"""
GmmTrainer runs through the .memmap file created by gmm_model.py, passes its data into PyTorch, and trains a probablistic 
word embedding model using the energy math from gmm_model.py. 
It outputs a .npz file containing the fully-trained model.
"""
class GmmTrainer():
    def train_model():
        return